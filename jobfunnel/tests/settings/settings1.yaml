# paths
output_path: 'fish'

# providers from which to search (case insensitive)
providers:
  - 'Indeed'
  - 'Monster'

# filters
search_terms:
  region:
    state: 'NY'
    city:     'New York'
    domain:   'com'